# DriverConnect Backend - Variáveis de Ambiente

# Configurações da aplicação
SECRET_KEY=asdf#FGSgvasgf$5$WGT
JWT_SECRET_KEY=jwt-secret-string-change-in-production
PORT=5000

# Configurações do banco de dados MySQL (Railway)
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=sua_senha_aqui
DB_NAME=driverconnect

# Para Railway, use as variáveis que o Railway fornece automaticamente:
# DB_HOST será fornecido pelo Railway
# DB_PORT será fornecido pelo Railway  
# DB_USER será fornecido pelo Railway
# DB_PASSWORD será fornecido pelo Railway
# DB_NAME será fornecido pelo Railway

# Configurações de produção
FLASK_ENV=production
DEBUG=False